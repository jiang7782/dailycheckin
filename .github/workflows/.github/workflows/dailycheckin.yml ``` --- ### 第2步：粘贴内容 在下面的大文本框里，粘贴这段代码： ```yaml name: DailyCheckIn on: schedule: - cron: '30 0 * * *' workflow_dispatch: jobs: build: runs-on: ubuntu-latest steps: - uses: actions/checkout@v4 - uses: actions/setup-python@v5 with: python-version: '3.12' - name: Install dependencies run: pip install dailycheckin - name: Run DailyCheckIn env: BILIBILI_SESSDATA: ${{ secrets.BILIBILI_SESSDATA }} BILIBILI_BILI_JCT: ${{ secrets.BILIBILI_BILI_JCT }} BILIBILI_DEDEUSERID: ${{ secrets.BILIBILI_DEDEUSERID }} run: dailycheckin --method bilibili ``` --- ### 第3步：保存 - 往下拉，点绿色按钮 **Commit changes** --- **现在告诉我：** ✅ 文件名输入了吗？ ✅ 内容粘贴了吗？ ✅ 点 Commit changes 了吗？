dailycheckin.yml
``` 内容填这个（全部复制粘贴）： ```yaml
name: DailyCheckIn on: schedule: - cron: '30 0 * * *' workflow_dispatch: jobs: build: runs-on: ubuntu-latest steps: - uses: actions/checkout@v4 - uses: actions/setup-python@v5 with: python-version: '3.12' - name: Install dependencies run: pip install dailycheckin - name: Run DailyCheckIn env: BILIBILI_SESSDATA: ${{ secrets.BILIBILI_SESSDATA }} BILIBILI_BILI_JCT: ${{ secrets.BILIBILI_BILI_JCT }} BILIBILI_DEDEUSERID: ${{ secrets.BILIBILI_DEDEUSERID }} run: dailycheckin --method bilibili
``` ### 第4步：保存
- 点 **Commit changes** --- **保存后自动运行！** 🚀 点 **Actions** 标签就能看到运行状态了。 --- **现在试试！** 告诉我每一步看到了什么 📸
